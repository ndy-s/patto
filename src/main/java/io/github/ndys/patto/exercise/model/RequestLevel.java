package model;

/**
 * Defines the different levels of requests that can be processed by the handler chain.
 */
public enum RequestLevel {
    INFO,
    WARNING,
    ERROR
    // TODO: Add more request levels as needed for your application.
}
